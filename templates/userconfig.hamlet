<div .pgcontainer>
    $maybe mensagem <- msg
        <div .alert.alert-success>
            ^{mensagem}
    $nothing
    
    <h1>
        Configuração da conta
    <form .formconfig method=post action=@{UserConfigR}>
        ^{widget}
        <br>
        <input .btn.btn-primary type="submit" value="Salvar">